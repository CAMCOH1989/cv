version: '2'

services:
  postgres:
    restart: always
    image: postgres:latest
    ports:
      - 5482:5432
    environment:
      POSTGRES_USER: api
      POSTGRES_PASSWORD: hackme
      POSTGRES_DB: landos
